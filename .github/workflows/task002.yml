
name: Build app

on:
  - pull_request

jobs:
  build_app:
    name: Build JS app

    runs-on: ubuntu-latest

    steps:
      - name: Install dependencies
        uses: actions/checkout@v4

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: npm run build